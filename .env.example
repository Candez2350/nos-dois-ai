# GEMINI_API_KEY — obrigatório para o chat (IA)
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Supabase — obrigatório para cadastro, login e dados
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# SESSION_SECRET — obrigatório para login (assinatura do cookie de sessão).
# Local: .env.local. Vercel: Settings → Environment Variables.
# Para gerar um valor: no terminal rode "openssl rand -hex 32" e cole o resultado aqui.
SESSION_SECRET=""

# APP_URL (opcional)
APP_URL="MY_APP_URL"
